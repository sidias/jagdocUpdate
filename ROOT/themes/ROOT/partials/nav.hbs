<ul class="nav">
	{{#each pages}}
		<li {{#if this.active}} class="active" {{/if}}>
			<a href="{{this.url}}">{{this.page}}</a>
		</li>
	{{/each}}
    <li>
        <input type="search" id="jaggerySearch" style="margin-top: 20px;">
    </li>
</ul>